{-# LANGUAGE NoImplicitPrelude #-}

module Prelude.Source.GHC.Classes (
    (&&), (||), not, otherwise,
    Eq((==), (/=)),
    Ord(compare, (<), (<=), (>=), (>), max, min),
  ) where

import Prelude (
    (&&), (||), not, otherwise,
    Eq((==), (/=)),
    Ord(compare, (<), (<=), (>=), (>), max, min),
  )
