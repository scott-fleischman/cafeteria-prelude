{-# LANGUAGE NoImplicitPrelude #-}

module Prelude.Source.GHC.Num (
    Num((+), (-), (*), negate, abs, signum, fromInteger),
    subtract,
  ) where

import Prelude (
    Num((+), (-), (*), negate, abs, signum, fromInteger),
    subtract,
  )
