{-# LANGUAGE NoImplicitPrelude #-}

module Prelude.Source.GHC.Base (
    String,
    Monad((>>=), (>>), return, fail),
    Functor(fmap),
    id, const, (.), flip, ($), until,
    asTypeOf,
    map, (++),
    foldr,
    IO,
  ) where

import Prelude (
    String,
    Monad((>>=), (>>), return, fail),
    Functor(fmap),
    id, const, (.), flip, ($), until,
    asTypeOf,
    map, (++),
    foldr,
    IO,
  )
