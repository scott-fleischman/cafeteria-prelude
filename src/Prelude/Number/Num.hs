{-# LANGUAGE NoImplicitPrelude #-}

module Prelude.Number.Num (
    Int, Integer,
    Num((+), (-), (*), negate, abs, signum, fromInteger),
    subtract,
  ) where

import Prelude (
    Int, Integer,
    Num((+), (-), (*), negate, abs, signum, fromInteger),
    subtract,
  )
