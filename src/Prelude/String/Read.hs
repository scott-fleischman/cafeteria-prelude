{-# LANGUAGE NoImplicitPrelude #-}

module Prelude.String.Read (
    ReadS,
    Read(readsPrec, readList),
    reads, readParen, read, lex,
  ) where

import Prelude (
    ReadS,
    Read(readsPrec, readList),
    reads, readParen, read, lex,
  )

