{-# LANGUAGE NoImplicitPrelude #-}

module Prelude.Basic (
    Bool(False, True),
    Maybe(Nothing, Just),
    maybe,
    Either(Left, Right),
    either,
    Ordering(LT, EQ, GT),
    Char,
    fst, snd, curry, uncurry,
    id, const, (.), flip, ($), until,
    asTypeOf,
  ) where

import Prelude (
    Bool(False, True),
    Maybe(Nothing, Just),
    maybe,
    Either(Left, Right),
    either,
    Ordering(LT, EQ, GT),
    Char,
    fst, snd, curry, uncurry,
    id, const, (.), flip, ($), until,
    asTypeOf,
  )
